name = "tryon-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
# Non-secret environment variables (if any)

# Secret variables - set via `wrangler secret put GEMINI_API_KEY`
# GEMINI_API_KEY = "your-api-key"
